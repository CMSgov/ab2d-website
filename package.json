{
  "scripts": {
    "gulp": "gulp",
    "dev": "gulp updateUswds && gulp watch",
    "build": "gulp updateUswds && gulp compile"
  },
  "dependencies": {
    "gulp": "^5.0.0",
    "@uswds/compile": "^1.1.0",
    "@uswds/uswds": "^3.8.1"
  }
}